    import XCTest
    @testable import SwHello

    final class SwHelloTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(SwHello().text, "Hello, World!")
        }
    }
